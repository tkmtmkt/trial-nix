# .bash_profile

# Get the environment and startup programs
if [ -f ~/.profile ]; then
  . ~/.profile
fi

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
  . ~/.bashrc
fi

# vim: set ft=sh ts=2 sw=2 et:
